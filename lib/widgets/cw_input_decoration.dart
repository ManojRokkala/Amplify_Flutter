import 'package:flutter/material.dart';

class CwInputDecoration extends StatelessWidget {
  const CwInputDecoration({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
